const ExamplePuzzlePath*: string = "db/example.non"
const WhiteSymbol*: int = 0  # The cell is white (empty).
const BlackSymbol*: int = 1  # The cell is black (filled).
const UnknownSymbol*: int = 2  # The cell's state is not yet determined.
